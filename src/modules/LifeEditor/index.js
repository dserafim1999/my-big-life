import React from "react";

import Card from "../../containers/Card";

const LifeEditor = () => {
    return (
        <Card width={200} verticalOffset={50} horizontalOffset={50}>
            <p>Life Editor</p>
        </Card>
    )
};

export default LifeEditor;